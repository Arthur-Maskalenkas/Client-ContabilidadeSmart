import { render, screen } from 'utils/test-utils'

import DropdownMobile from '.'

describe('<DropdownMobile />', () => {
  it('should render the heading', () => {
    render(<DropdownMobile />)
  })
})
